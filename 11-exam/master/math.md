这是方差的核心性质清单（简洁好记）：

1. 平移不变性
若一组数据每个数都加/减同一个常数  a ，方差不变。
例：数据  x_1,x_2…x_n  方差为  S^2 ，则  x_1+a,x_2+a…x_n+a  方差仍为  S^2 。

2. 缩放性质
若一组数据每个数都乘同一个常数  k ，方差变为  k^2S^2 。
例：数据乘2，方差变为原来的4倍。

3. 常数列方差为0
所有数都相等时，波动为0，方差  S^2=0 。

4. 非负性
方差是“偏差平方的平均数”，因此  S^2 \geq 0 ，仅当所有数相等时取到0。
